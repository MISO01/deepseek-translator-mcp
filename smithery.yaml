name: deepseek-translator-mcp
startCommand:
  type: stdio
  configSchema: {}
  commandFunction: |
    function(config) {
      return {
        command: 'python3',
        args: ['server.py'],
        env: {}
      };
    }
